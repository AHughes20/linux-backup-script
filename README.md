# Linux Backup Script

Simple bash script that creates compressed backups of a specified directory.

## Usage
sudo ./backup.sh /path/to/dir

## Features
- Root privilege check
- Input validation
- Timestamped tar.gz backups
- Safe path handling
